"""
The `packaging` sub-package contains modules related to the construction and
verification of Progressive Secure Provider Format (PSPF) packages.

This includes:
- Orchestrating the build process, typically by invoking external tools like the Go builder CLI.
- Verifying the integrity and authenticity of PSPF packages.
"""
